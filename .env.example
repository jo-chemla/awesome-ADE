# Copy to .env, fill in once you've pushed this to your own GitHub repo.
# Used only to render the "Contribute or correct an entry" link — safe to
# leave unset, the link just won't render.
VITE_REPO_URL=https://github.com/<you>/<repo>
