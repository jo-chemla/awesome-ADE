---
key: bb
name: "bb"
category: "CLI orchestrator"
website: "https://getbb.app"
github: "https://github.com/get-bb/bb"
license: "MIT, open source"
platform:
  windows: beta
  linux: beta
  macos: full
  android: none
  ios: none
pfNote: "Native one-click install on macOS; Windows/Linux run it via WSL"
parallel: "Multiple concurrent agent threads, delegation across providers (Claude, OpenAI, Cursor, etc.)"
isolation: "Each thread keeps its own independent execution context"
locality: "Local-first, plus remote-machine access"
remote: "CLI-triggerable from scripts/cron, and via chat-bot integrations (Telegram, Signal, Slack)"
mobileCtl: "No"
---

Billed as "the agent IDE that builds itself" — runs on your own AI provider
subscriptions (billed by them, not bb), fully forkable/customizable end to
end.
